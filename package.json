{
  "name": "portfolio-caresle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:projects": "node scripts/build-projects.js",
    "build:experiences": "node scripts/build-experiences.js",
    "build:skills": "node scripts/build-skills.js",
    "build:about": "node scripts/build-about.js",
    "build:data": "npm run build:projects && npm run build:experiences && npm run build:skills && npm run build:about",
    "dev": "npm run build:data && vite",
    "build": "npm run build:data && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.8"
  }
}
